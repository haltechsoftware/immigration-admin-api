import { CheckpointCategoryModule } from './category/checkpoint_category.module';
import { CheckpointModule } from './checkpoint/checkpoint.module';

export const checkpointModules = [CheckpointCategoryModule, CheckpointModule];
